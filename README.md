# Airflow Project
A robust, containerized Apache Airflow environment configured for orchestrating multi-stage data pipelines. This project utilizes uv for lightning-fast Python package management and Docker Compose to orchestrate Airflow alongside PostgreSQL (metastore) and Redis (Celery backend).

# 🚀 Features
Modern Python Tooling: Uses uv and pyproject.toml for fast, deterministic dependency resolution.

Production-Ready Stack: Built on Docker Compose with Celery Executor, utilizing PostgreSQL for state tracking and Redis for task queuing.

Modular DAG Design: Features an orchestrated workflow structure, including a master/parent DAG pattern to trigger interdependent data pipelines cleanly.

## 🛠️ Prerequisites
Before running this project, ensure you have the following installed:

Docker & Docker Compose

uv (for local development and environment management)

Python (matching the version specified in .python-version)

## 📂 Project Structure

```text
├── .github/               # CI/CD workflows (if applicable)
├── airflow/
│   ├── dags/             # Airflow Directed Acyclic Graphs (DAGs)
│   │   ├── parent_dag.py # Master orchestrator DAG 
│   │   └── ...           # Core ETL / pipeline DAGs
│   ├── .gitignore        # Prevents tracking local airflow.cfg, logs, and plugins
│   ├── .python-version   # Pinpoints local Python environment version
│   ├── docker-compose.yaml # Local multi-container Airflow stack
│   ├── main.py           # Application entry point / helper scripts
│   ├── pyproject.toml    # Python project metadata and dependencies
│   └── uv.lock           # Locked dependency tree generated by uv
└── README.md


